# Where the browser-facing server talks to the platform. Server-side only: the browser
# never receives this value, and never holds an access token.
GATEWAY_URL=http://localhost:8080
IDENTITY_URL=http://localhost:8081

# Set in any deployment served over TLS so the session cookie is marked Secure.
COOKIE_SECURE=false

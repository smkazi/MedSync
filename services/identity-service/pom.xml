<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.hms</groupId>
    <artifactId>hms-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>identity-service</artifactId>
  <name>identity-service</name>
  <description>Authentication, users, roles and the platform audit trail</description>

  <dependencies>
    <dependency>
      <groupId>com.hms</groupId>
      <artifactId>hms-common</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-oauth2-jose</artifactId>
    </dependency>
    <!-- BouncyCastle backs Argon2 password hashing.
         Nothing in this repository imports org.bouncycastle, which is why it looks removable and is
         not: Spring Security's Argon2PasswordEncoder needs
         org.bouncycastle.crypto.params.Argon2Parameters, so deleting this fails every login with a
         NoClassDefFoundError at bean creation. Tried it; 15 integration tests said no.

         Deliberately unversioned. It used to be pinned at 1.82 here, which carried
         CVE-2025-14813 (CRITICAL) and CVE-2026-5598 (HIGH - private-key leakage through
         non-constant-time comparison); the SBOM scan added to CI caught both on its first real run.
         The version now comes from the imported spring-cloud-dependencies BOM, which already
         supplies 1.85.2 transitively to the gateway through spring-cloud-starter. A local pin was
         what let this drift three releases behind the rest of the reactor in the first place, and
         it would have left two different bcprov versions in one build. -->
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-kafka</artifactId>
    </dependency>
    <!-- Spring Boot 4 moved auto-configuration into per-technology modules: the bare
         flyway-core jar carries no auto-configuration, so the starter is required. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
